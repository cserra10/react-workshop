{
  "name": "classroom-mexico-firebase",
  "version": "1.0.2",
  "description": "Cloud Firestore emulator testing",
  "scripts": {
    "emulate": "firebase emulators:start",
    "format": "prettier --write **/*.js",
    "test": "mocha --timeout=10000"
  },
  "devDependencies": {
    "@firebase/testing": "^0.15.0",
    "axios": "^0.19.2",
    "chance": "^1.1.4",
    "cors": "^2.8.5",
    "firebase-admin": "^8.11.0",
    "mocha": "5.2.0",
    "prettier": "1.15.2"
  }
}
